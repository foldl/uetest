{ue_port, 20}.
{pathloss, {6, 6}}.
{plmn, "00101"}.
{auth, on}. % on, off

{restart, cfun}. % cfun, modem_reset, reboot

{tester_port, [vxi11, {"10.0.150.192","inst1"}]}. % {tester_port, [gpib]}.
{tester, '3110'}.
